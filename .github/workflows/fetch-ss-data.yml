# # .github/workflows/fetch-ss-data.yml
# name: Daily SS Data Fetch

# # 定时触发和手动触发都支持
# on:
#   schedule:
#     # cron 格式：分钟 小时 日 月 周
#     # 下面这行表示：UTC 时间每天 02:00 触发一次
#     - cron: '0 2 * * *'
#   workflow_dispatch:  # 同时也允许手动在 Actions 页面点「Run workflow」

# jobs:
#   update-ss-data:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '16'  # 根据你项目需要改成 14/18/... 都行

#       - name: Install dependencies
#         run: npm install

#       - name: Run fetch_ss_data
#         run: npm run fetch_ss_data
#         # 如果你是直接用 Python 脚本，可以改成：python scripts/fetch_ss_data.py

#       - name: Commit and push updated data
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"
#           git add _data/ss_data.json
#           # 只有在有改动的时候才 commit
#           git diff --cached --exit-code || git commit -m "chore: daily update ss_data"
#           git push
#         env:
#           # 如果你的 repo 需要写权限，用内置 GITHUB_TOKEN 就可以推送
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}